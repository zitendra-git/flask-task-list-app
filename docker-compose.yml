services:
  flask-app:
    build: .    # Uses the Dockerfile in the current directory
    ports:
      - "5000:5000"

#docker-compose up --build
#image: zitendra/task-list-app:latest    # Uses the Dockerfile in the current directory
